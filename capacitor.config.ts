import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drunkgames.simulator',
  appName: 'Drunk Simulator',
  webDir: 'dist'
};

export default config;
